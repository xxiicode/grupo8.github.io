const pcolor = require("picocolors") //si no lo declaro en el modulo no anda
console.log(pcolor.green("chau!"));

const mensajeTest = "esto es un mensaje de test";

module.exports = mensajeTest;

// para el localhost:
// PORT=3000
// DB_NAME=localhost_mago_db
// DB_USER=root
// DB_PASS= 
// DB_HOST=localhost